/**
 * Declares the application name shown across shared CLI surfaces.
 */
export const APP_NAME = "Cortex";

/**
 * Provides the placeholder response used before the real runtime path is finalized.
 */
export const DEFAULT_FALLBACK_MESSAGE =
  "Codex SDK is wired as the MVP integration point, but the runtime call path still needs to be finalized against your exact SDK usage.";
